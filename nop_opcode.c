#include "monty.h"

/**
 * nop - Does nothing; a placeholder for the "nop" opcode.
 *
 * This function is a placeholder for the "nop" opcode in Monty language.
 * It does nothing, maintaining opcode handling consistency.
 *
 * @stack: Ptr to the pointer to the stack's top node (unused).
 * @line_number: The line number from the Monty file (unused).
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
