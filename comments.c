#include "monty.h"

/**
 * comments - Handles comments in the Monty file.
 * @stack: A pointer to the top of the stack.
 * @line_number: The current line number in the Monty file.
 *
 * Description:
 * This function is a placeholder for handling comments in the Monty file.
 * It takes in the stack pointer, the current line number but doesn't perform
 * any operations on the stack. It is used to skip lines in the Monty file that
 * are comments.
 *
 * Return: None.
 */

void comments(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
